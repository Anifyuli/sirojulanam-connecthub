{
  "name": "sirojulanam-connecthub",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --filter=!api run dev",
    "dev:all": "pnpm run dev & pnpm --filter api run dev",
    "dev:api": "pnpm --filter api run dev",
    "build": "pnpm -r run build",
    "preview": "pnpm -r run preview",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "devDependencies": {}
}
